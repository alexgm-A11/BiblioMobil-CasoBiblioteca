# Evidencias de BiblioMobil

- `inicio-claro.png`: portada en modo claro.
- `inicio-oscuro.png`: portada en modo oscuro.
- `libros-cargando.png`: fase `Cargando`.
- `libros-sin-libros.png`: fase `SinLibros`.
- `libros-con-libros.png`: fase `ConLibros`, conteo, formato y badge de reposición.
- `libros-error.png`: fase `Error` con mensaje y acción para reintentar.
- `lectores-telefono-ausente.png`: lector registrado sin teléfono y texto `No registrado`.
- `prestamos.png`: módulo de préstamos en construcción.

Para las capturas `Cargando` y `Error` se modificó temporalmente el repositorio, tal como permite el enunciado. El código final fue restaurado y no contiene esas modificaciones.
